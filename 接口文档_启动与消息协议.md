# 项目启动与接口文档（WebSocket + Orleans）

## 1. 工程启动位置

## 1.1 实际启动入口
- 启动项目：`Project_Mouse_Server_CSharp`
- 启动对象：`CLIP.Server.Server_Main_ver_02`
- 依据：`Project_Mouse_Server_CSharp/Project_Mouse_Server_CSharp.csproj` 中：
  - `OutputType = Exe`
  - `StartupObject = CLIP.Server.Server_Main_ver_02`

## 1.2 主入口方法
- 文件：`Project_Mouse_Server_CSharp/Server_Main_ver_02.cs`
- 方法：`public static async Task Main(string[] args)`
- 主要流程：
  1. 加载游戏数据（`DataManager.LoadData()`）
  2. 初始化日志（Serilog）
  3. 加载配置与静态缓存
  4. 启动 IMGUI（`Server_GUI_Renderer`）
  5. 由 GUI 触发 `_start_server` 事件后执行 `start_server()`

## 1.3 服务启动链路（start_server）
- 仍在：`Project_Mouse_Server_CSharp/Server_Main_ver_02.cs`
- 核心动作：
  1. 计算对外 IP（本机或 GUI 配置）
  2. 启动 WebSocket 服务：`WebSocketSharp_Server.start_server("ws://<ip>:25564/")`
  3. 绑定消息事件：
     - `WebSocketSharp_Server_Event._on_message -> Msg_Dispatcher.receive_msg`
     - `WebSocketSharp_Server_Event._after_disconnect -> Server_Helper_Function.after_closing_player_connection`
  4. 初始化 PostgreSQL 数据源
  5. 启动 Orleans（localhost clustering）
  6. 初始化 `IClusterClient / IGrainFactory / IMsg_Sender`
  7. 将 dispatcher 与 helper 组件绑定到 Orleans 与 WS sender

## 1.4 另一个 Main（非主入口）
- 文件：`Project_Mouse_Server_Network/WS_Server.cs`
- 说明：包含一个测试用 `Main`，但不是当前可执行程序的 StartupObject。

---

## 2. 通信架构与接口设计

## 2.1 对外协议类型
- **无 HTTP Controller/REST 接口**（工程内未发现 ASP.NET Controller 路由）
- **对外接口为 WebSocket 消息协议**
- WebSocket 地址：`ws://<server_ip>:25564/`
- WebSocket 服务路径：`/`

## 2.2 总体分发设计
1. 客户端通过 WebSocket 发送文本或二进制（LZ4）消息。
2. `WebSocketSharp_Server_Event._on_message` 回调收到 `(connectionId, messageString)`。
3. `Msg_Dispatcher.try_dispatcher_msg` 反序列化为 `Network_Msg`：
   - `action == "Try_Login"` -> `Login_Manager_ver_02.try_login(connectionId, msg)`
   - `action_target` 包含 `"Player_Server"` -> 投递到玩家 Grain：`IPlayer_Server_Grain_ver_02.receive_msg(msg)`

## 2.3 消息包结构（Network_Msg）
> `Network_Msg` 类型定义不在当前仓库源码中（应来自外部依赖），以下字段由使用点归纳：

- `player_id`：玩家标识（字符串）
- `msg_id`：消息序号（整数）
- `sender`：发送方模块（如 `Player_Server`、`Login_Manager`）
- `action_target`：目标模块（如 `Player_Server`、`Global_Game_Manager`）
- `action`：动作名（核心路由键）
- `detail_info`：业务负载（多为 JSON 字符串）
- `_sending_mode`：发送模式（常见值：`Server_to_Client`）

---

## 3. 接口文档（客户端 -> 服务端）

以下接口均为 WebSocket 消息，除登录外大部分通过 `Player_Server_Grain_ver_02.receive_msg` 处理。

## 3.1 认证与会话

### 3.1.1 Try_Login
- action: `Try_Login`
- action_target: 通常应指向 `Login_Manager`（由分发器按 action 处理）
- detail_info: `List<string>`，格式：`[userName, password]`
- 处理器：`Login_Manager_ver_02.try_login`
- 返回：
  - `action = "Login_Success"`，`detail_info` 示例：
    - `Create_New_Account_#_Login_Success_#_<playerId>`
    - `Password_Correct_#_Login_Success_#_<playerId>`
  - `action = "Login_Failure"`，`detail_info` 示例：
    - `Password_Incorrect_#_Login_Failure`

---

## 3.2 通用数据读写（旧协议）

### 3.2.1 Get_Data
- action: `Get_Data`
- detail_info: 字符串类型，按关键字分支
- 支持查询项：
  - `Game_Inventory`
  - `Room_Data`
  - `Player_LoginReward`
  - `Indoor_Data`（含 `Living_Room` / `Bed_Room` / `Toilet` / `Balcony`）
  - `Planting_Data`
  - `Main_Character_Cloth`
  - `Weather_State`
  - `Dispatch_Info`
  - `Shop_State`
  - `Player_Brief`
- 返回：统一 `action = "Response_Data"`，`detail_info` 多为 `List<string>[数据类型, 数据内容]`

### 3.2.2 Save_Data
- action: `Save_Data`
- detail_info: `List<string>[dataType, dataPayload]`
- 支持写入项与字段映射：
  - `Game_Inventory` -> `player.player_inventory`
  - `Room_Data` -> 房间明细
  - `Player_LoginReward` -> `player.player_loginreward`
  - `Indoor_Data.*` -> 各 indoor 字段
  - `Planting_Data` -> `player.planting_state`
  - `Main_Character_Cloth` -> `player.main_character_cloth`
  - `Weather_State` -> `player.weather_info`
  - `Dispatch_Info` -> `player.dispatch_state`
  - `Shop_State` -> `player.shop_state`
  - `Player_Brief` -> `player.player_brief`（并更新 `affinity_with_main_character`）

### 3.2.3 Get_Friend_Data
- action: `Get_Friend_Data`
- detail_info: `List<string>[friendName, dataTarget]`
- 支持：好友室内数据、种植数据
- 返回：`Response_Data`（与 Get_Data 同风格）

### 3.2.4 Save_Friend_Data
- action: `Save_Friend_Data`
- detail_info: `List<string>[friendName, dataTarget, data]`
- 当前可见支持：`Planting_Data`

---

## 3.3 社交接口

### 请求 action
- `Try_Find_Friend`：按好友 ID 查询
- `Try_Add_Friend`：发起好友申请
- `Get_Social_Info`：拉取玩家社交信息
- `Confirm_Friend`：同意好友申请
- `Refuse_Friend`：拒绝好友申请
- `Remove_Friend`：删除好友
- `Post_Chat`：发送聊天内容
- `Get_Chat`：拉取与某好友聊天记录
- `Send_Present`：赠送礼物
- `Save_Present_Records`：保存礼物记录
- `Visit_Friend_Room`：记录访问好友房间行为

### 主要响应 action
- `Response_Find_Friend`
- `Response_Social_Info`（推送目标：`Player_Social_Receiver`）
- `Response_Social_Chat_Info`（推送目标：`Player_Social_Receiver`）

---

## 3.4 邮件与公告

### 请求 action
- `Get_Mail`
- `Get_Anno`
- `On_Read_Mail`（detail_info: `List<int> mailIds`）
- `On_Delete_Mail`（detail_info: `List<int> mailIds`）
- `On_Get_Mail_Reward`（detail_info: `List<int> mailIds`）

### 响应 action
- `Response_Mail`（目标：`Email_And_Announcement_Receiver`）
- `Response_Anno`（目标：`Email_And_Announcement_Receiver`）

---

## 3.5 成就系统

### 请求 action
- `Get_Achievement_Data`
- `On_Get_Achievement_Reward`

### 响应/推送 action
- `Response_Achievement_Data`（目标：`Quest_And_Achievement_Receiver`）
- `New_Achievement_Unlocked`（目标：`Quest_And_Achievement_Receiver`）

---

## 3.6 货币系统（两套接口并存）

### 方案 A（旧）
- `Get_Currency` -> 响应 `Response_Currency_All`
- `Add_Currency` -> 响应 `Response_Currency_Update`
- `Minus_Currency` -> 响应 `Response_Currency_Update`

### 方案 B（新，统一 SendData）
- `ChangeCurrency`
- `LoadCurrency`
- `ChangeCurrencyMulti`（当前代码中未启用）
- 响应行为：`SendData(action, result)`，action 与请求同名，目标为 `Global_Game_Manager`

---

## 3.7 NPC 系统

### 请求 action
- `Get_All_NPC_Data`
- `Get_Player_NPC_Data`
- `Give_Gift_To_NPC`
- `Meet_NPC_Event`

### 响应/推送 action
- `Response_Player_NPC_Data`（目标通常为 `NPC_Receiver` 或请求 sender）
- `NPC_Send_Gift_TO_Player`（目标：`NPC_Receiver`）

---

## 3.8 玩家成长、时间、照片、植物

### 玩家成长
- `Save_Player_Level`
- `Load_Player_Level`

### 时间
- `GetTime`

### 照片
- `Upload_Photo`（旧接口）
- `Get_Photo`（旧接口，返回 `Send_Photo`）
- `SavePhoto`（新接口）
- `LoadPhoto`（新接口）

### 植物
- `UpdatePlant`
- `LoadPlant`（与 UpdatePlant 走同逻辑）
- `WaterPlant`
- `PlantPlant`
- `HarvestPlant`
- `RemovePlant`
- `FertilizePlant`

> 上述新接口（Time/Photo/Plant/Currency）大多通过 `SendData(action, data)` 返回，`action_target = Global_Game_Manager`。

---

## 4. 服务端主动下发（Server -> Client）

- `Quit_Game`（顶号或强制下线，目标：`Global_Game_Manager`）
- `Response_Social_Info`
- `Response_Social_Chat_Info`
- `Response_Mail`
- `Response_Anno`
- `Response_Achievement_Data`
- `New_Achievement_Unlocked`
- `NPC_Send_Gift_TO_Player`
- `Send_Photo`

---

## 5. 接入建议（给客户端）

1. 所有请求统一封装 `Network_Msg`，保证字段完整。
2. `action` 作为唯一业务路由键；`detail_info` 建议统一 JSON 编码。
3. 登录成功后保存服务端分配的会话上下文，处理被顶号时的 `Quit_Game`。
4. 新旧接口并存（尤其货币/照片/植物），客户端最好按版本做分流。
5. 建议后续补充一份“`detail_info` 的 JSON Schema 文档”，便于前后端严格校验。


# 模拟客户端（Project_Mouse_SimClient）使用说明

## 1. 目的

`Project_Mouse_SimClient` 是一个用于连接本地服务器的 **C# 控制台模拟客户端**：

- 启动后自动连接 WebSocket 服务器并发送登录（账号可自定义，密码固定 `123456`）
- 进入命令行交互模式，按 `switch-case` 解析你输入的指令并组包发送
- 持续接收服务端消息并打印到控制台（支持服务端 LZ4 二进制包解压打印）

项目位置：`d:\Unity\Server\Project_Mouse_SimClient\`

---

## 2. 前置条件

- 已安装 `.NET SDK 8`（工程 `net8.0`）
- 服务器已启动并开始监听 WebSocket（默认端口 `25564`）

> 注意：服务器进程启动后 **不代表** WebSocket 已监听。
> 该工程默认需要在 GUI 里点击 `Start_Server` 才会真正开始监听端口。

---

## 3. 启动服务器（建议流程）

在 Cursor 终端执行（PowerShell）：

```powershell
dotnet run --project d:\Unity\Server\Project_Mouse_Server_CSharp\Project_Mouse_Server_CSharp.csproj
```

当服务器 WebSocket 真正启动时，日志通常会出现类似：

- `Begin_Start_Server`
- `WebSocketSharp_Server_start_server()_at_ws://<ip>:25564/`

然后用下面命令确认端口已监听：

```powershell
netstat -ano | findstr ":25564"
```

看到 `LISTENING` 才表示客户端可以连接。

---

## 4. 运行模拟客户端

### 4.1 基本命令

```powershell
dotnet run --project d:\Unity\Server\Project_Mouse_SimClient\Project_Mouse_SimClient.csproj -- <账号名> [ws地址]
```

- **账号名**：必填（例如 `Alice`）
- **密码**：固定 `123456`（写死在模拟器里）
- **ws地址**：可选，默认 `ws://127.0.0.1:25564/`

示例：

```powershell
dotnet run --project d:\Unity\Server\Project_Mouse_SimClient\Project_Mouse_SimClient.csproj -- Alice ws://127.0.0.1:25564/
```

### 4.2 自动登录行为

连接成功后会自动发送：

- `action = "Try_Login"`
- `detail_info = ["<账号名>", "123456"]`（JSON 数组字符串）

---

## 5. 控制台指令格式

### 5.1 通用格式

在模拟器控制台里输入一行：

```
action 参数1 参数2 ...
```

模拟器会用 `switch-case` 做一部分常用指令的专门处理；其余指令走默认分支：

- 默认 `action_target = "Player_Server"`
- `detail_info` 为你输入的剩余参数拼接成的字符串

### 5.2 内置指令（已实现 switch-case）

- `help`
- `relogin`
- `quit` / `exit`
- `Get_Social_Info`
- `Try_Find_Friend <friend_id_int>`
- `Try_Add_Friend <friend_id_int>`
- `Confirm_Friend <friend_user_name>`
- `Refuse_Friend <friend_user_name>`
- `Remove_Friend <friend_user_name>`
- `Get_Chat <friend_user_name>`
- `raw <action_target> <action> [detail_info]`

`raw` 示例：

```
raw Player_Server Get_Data "Game_Inventory"
```

---

## 6. 收发包日志说明

模拟器会打印：

- **发送**：`[WS] => <json>`
- **接收 text**：`[WS] TEXT <= <text>`
- **接收 binary（LZ4）**：`[WS] BIN(LZ4) <= <decoded string>`
- **关闭**：`[WS] CLOSE code=... reason=... wasClean=...`

---

## 7. 常见问题

### 7.1 连接报错 `SocketException (10061)`（连接被拒绝）

含义：目标地址/端口 **没有服务在监听**。

排查步骤：

1. 先确认服务器端口是否在监听：

```powershell
netstat -ano | findstr ":25564"
```

2. 如果服务器日志里的启动地址不是 `127.0.0.1`，请用日志里那个地址连接。

例如服务器打印：`ws://192.168.31.10:25564/`，则客户端也要用：

```powershell
dotnet run --project d:\Unity\Server\Project_Mouse_SimClient\Project_Mouse_SimClient.csproj -- Alice ws://192.168.31.10:25564/
```

### 7.2 能连接但没有业务回包

通常是：

- 登录失败（检查服务端 `Login_Manager_ver_02` 的响应）
- 发送的 `action_target/action/detail_info` 不符合服务端分发逻辑（见 `Msg_Dispatcher` 与 `Player_Server_Grain_ver_02`）

---

## 8. 相关代码入口

- 模拟器入口：`Project_Mouse_SimClient/Program.cs`
- 服务端分发：`Project_Mouse_Server_CSharp/Msg_Dispatcher.cs`
- 登录处理：`Project_Mouse_Server_CSharp/Login_Manager_ver_02.cs`
- WebSocket 服务器：`Project_Mouse_Server_Network/WebSocketSharp_Server.cs`

